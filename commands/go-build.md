# /go-build - Go Build Error Resolution Command

## Description
Fixes Go-specific build errors using the Go build resolver agent.

## Usage
```
/go-build
/go-build --module-issues
/go-build --cross-compile
```
